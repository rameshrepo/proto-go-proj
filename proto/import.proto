syntax = "proto3";

package example.simple;

import "one.proto";  // Using message from another proto file belonging to same package

option go_package = "github.com/rameshrepo/proto-go-proj/proto";

message import {
    MessageOne one = 1;
}
